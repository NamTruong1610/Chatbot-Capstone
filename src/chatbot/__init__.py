"""Configurable RAG pipeline and evaluation harness.

This is a research artefact: the deliverable is a set of switchable, versioned
configurations plus a harness that measures them, not a chatbot. See ``CLAUDE.md`` and
``docs/``. Only the configuration system (``chatbot.config``) exists so far; every other
subpackage is introduced in its own build-plan phase (``docs/07-build-plan.md``).
"""